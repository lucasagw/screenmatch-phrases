package br.com.alura.screenmatch_phrases.dto;

public record PhraseDTO(String title,
                        String text,
                        String character,
                        String poster) {
}
